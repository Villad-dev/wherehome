
import 'package:flutter/material.dart';

class PhoneValidator extends StatelessWidget {
  const PhoneValidator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
